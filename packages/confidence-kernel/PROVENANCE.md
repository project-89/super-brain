# Provenance

The implementation, tests, README, and license in this directory were imported
on 2026-08-14 from:

- repository: `git@github.com:project-89/confidence-kernel.git`
- commit: `aa86bf8b27fbcbba2f6c4c2ce109deefa28ad402`
- package version: `0.2.0`
- license: MIT

At import time the source repository had one untracked file,
`REPORT_KERNEL.md`. A `git diff HEAD` check over `src`, `test`, `README.md`, and
`LICENSE` was clean. The untracked report, source lockfile, and packed npm
artifact were not imported.

`src/`, `test/`, `README.md`, and `LICENSE` were copied without modification.
`package.json` and `tsconfig.json` are workspace-owned build metadata.
The package tsconfig retains the source release's optional-property semantics by
overriding the repository-wide `exactOptionalPropertyTypes` setting.
