In event-driven systems, ingestion position, canonical event time, and occurrence identity answer different questions. An ingestion position records delivery progress; event time orders the domain history; an immutable event ID identifies a logical occurrence. A delayed event can have an older event time and still be newly delivered. Decimal sequence positions can exceed JavaScript's safe-number range, so use an exact representation when comparing or advancing them.
