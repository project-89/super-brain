/** Synthetic exercise fixture. All task input is supplied in the prompt. */
export function reduceDelivery(state, arrivals) {
  throw new Error("Implement reduceDelivery according to the supplied contract");
}
